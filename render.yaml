services:
  - type: web
    name: shop-platform
    env: node
    plan: free
    buildCommand: "echo 'No build dependencies needed'"
    startCommand: "node server.js"
    envVars:
      - key: NODE_VERSION
        value: 22.0.0
      - key: PORT
        value: 10000
      - key: HOST
        value: 0.0.0.0
      - key: DATABASE_PATH
        value: ./shop.db
